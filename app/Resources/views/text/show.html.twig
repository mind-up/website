{% extends 'base.html.twig' %}

{% block body %}
    <h1>Text</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ text.id }}</td>
            </tr>
            <tr>
                <th>Textid</th>
                <td>{{ text.textId }}</td>
            </tr>
            <tr>
                <th>French</th>
                <td>{{ text.french }}</td>
            </tr>
            <tr>
                <th>Editable</th>
                <td>{% if text.editable %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('text_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('text_edit', { 'id': text.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
